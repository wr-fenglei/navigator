interface:
  display_name: "导航者"
  short_description: "用苏格拉底式提问明确目标, 拆解实施循环, 由主 agent 调度执行和验证"
  default_prompt: "使用 $navigator 处理当前任务, 逐步明确目标和做法, 按本次范围推进实施与验证"
policy:
  allow_implicit_invocation: true
