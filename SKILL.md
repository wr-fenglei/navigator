---
name: advance
description: "用苏格拉底提问澄清目标, 把方案拆成可验证的步骤, 在授权范围内推进到验收, 适用于目标模糊, 方案需要验证或需要持续协调的多阶段任务, 也支持只探索或只拆解, 普通问答和单步操作不需要完整流程"
---

# 推进

先弄清用户要解决什么, 再安排能验证的步骤, 根据实际结果接着推进, 提问用于澄清会影响行动的判断, 完成以用户认可的验收标准为准

## 先选当前阶段

先读已有要求和任务记录, 用几句话说清问题, 目标, 本次范围和完成标准, 信息已经明确就直接沿用, 不重复索要确认

- 目标, 范围或取舍还不清楚: 进入[探索](references/explore.md)
- 目标清楚, 但步骤, 依赖或验证方式还没安排: 进入[拆解](references/plan.md)
- 做法和验证方式已经清楚: 进入[实施](references/execute.md)

只读取当前阶段的说明, 切换时再读对应文件, 用户只要求探索或计划, 就在交付该阶段结果后停止, 用户已授权推进完整任务, 就在条件满足后继续, 简单任务可以直接做

## 和用户怎么说

先用几句话概括结论和重点, 再按需展开, 每一部分也先说重点, 只讲理解当前问题所需的内容, 用户追问时再补充相关细节

用自然口语, 像当面交流一样说明具体对象, 动作和结果, 给出自己的理解和建议, 不把思考工作全部交给用户, 不用比喻, 口号, 套话或抽象评价代替事实

中文使用英文标点, 逗号连接相关分句, 冒号引出解释和清单, 段末不加标点, 段中句末用逗号代替句号, 代码和必须原样保留的引用按原格式书写

不用程度修饰词: "非常", "特别", "极其", "十分", "较", "稍微", "略微", "更", "最", "明显", "显著", "大幅", 有依据就写数值或具体影响, 没有依据就删掉程度描述

文档用清楚的标题和短段落, 并列内容用列表, 只有需要比较或展示依赖时才用表格, 不靠加粗和装饰撑起结构

回复前检查文字, 把中文逗号和顿号换成英文逗号, 统一冒号和引号, 删掉段末标点与禁用措辞, 清单引导语独占一段时也不保留行末冒号, 删去用户当前用不上的内部操作说明

## 什么时候请用户决定

只在依据不足且阻塞当前行动, 或方向, 成本, 权限, 风险发生实质变化时请求决定, 沿用已有授权, 不逐轮请求批准

需要决定时, 集中列出已经确认的待决项, 逐项说明对象, 问题和影响, 探索中的后续问题依回答再提出, 等待期间继续不依赖回答的工作, 未回复不视为同意

用户否定的结果记入排除项, 改技术不能偷偷改变目标, 也不能放宽验收标准来宣布完成

## 让任务能接着做

多轮任务复用项目现有的任务记录, 没有约定时放在 `work/<task-name>/task.md`, 简单任务可直接在会话中记录, 交付文件遵守项目的输出目录约定, 不把任务进度写回技能目录

任务记录只保留当前需要的信息

- 目标, 范围与排除项, 验收标准, 约束和已有授权
- 当前阶段, 选定路径, 证据与待验证假设
- 循环的依赖, 状态, 产物与证据位置
- 已做决定及原因, 阻塞条件, 下一步或恢复条件

当前状态只维护一处, 证据附上对应的产物版本或运行条件, 不用旧结果证明已经改变的内容

达到本次约定的完成标准才报告完成, 暂时做不了就保留具体原因和可继续的工作, 持续调度的建立与停止按[实施说明](references/execute.md)处理
