---
name: navigator
description: "用苏格拉底式提问明确目标, 拆出多个关联且可验证的循环, 通过 SQLite 保存过程和当前状态, 协调实施直到验收, 也支持只讨论或只做计划"
---

# 导航者

先弄清用户想得到什么, 再拆出能独立实施和验证的循环, 主 agent 管方向和安排, 循环任务负责实施, 独立 subagent 检查结果, 数据库保存过程与当前状态

## 为什么这样设计

先根据已有信息说出理解和建议, 再用苏格拉底式提问检查含义, 假设, 依据和取舍, 用户举的例子不自动限定全部用途, 已经明确的要求直接沿用

计划可以调整, 每次尝试的依据, 结果和改动原因都要留下, 正在尝试的方案不自动替代当前决定, 平时先读当前情况, 需要核实再查历史

技能说明判断原则, 脚本负责记录关系和检查更新条件, 验证内容是否满足用户要求, 仍由独立验证者检查

## 怎么推进

1. 明确目标, 排除项, 验收和约束, 先检查会推翻方案的未知项, 失败后根据新依据换方案, 不靠继续拆分保证可行
2. 拆出多个相对独立又相互关联的循环, 写清结果, 输入, 修改范围, 通过条件和依赖, 按任务需要单列质量检查, 应急准备和整体验收循环
3. 保存蓝图和计划, 按依赖与阶段派发, 每个循环交回产物后由新上下文的 subagent 独立验证, 验证通过才启动依赖它的工作
4. 失败先修复再按原标准验证, 路径失效就调整计划, 涉及目标, 验收或用户取舍时, 带着具体方案请用户决定
5. 暂停后先核对任务与产物再继续, 当前版本通过整体验收后交付, 主观体验由用户确认

用户明确要求按循环新建任务时, 一个循环创建一个新任务, 其内部用 subagent 验证, 普通子任务用 subagent, 验证者不再派发验证者, 已有授权不重复询问

简单任务直接处理, 用户只要讨论或计划就停在该范围, 明确要求稍后继续或定时运行时, 使用环境提供的自动化工具, 工具确认成功后才说明已安排

## 记录和交接

本地记录统一放在项目或当前任务目录的 `.navigator/state.sqlite`, 各循环使用主 agent 指定的同一绝对路径, 产物保留在实际工作目录, 通过路径和内容校验值关联

- 操作数据库时读[角色与命令](references/commands.md), 使用 `scripts/navigator.py`, 不手改数据库状态
- 派发循环或独立验证时读[交接格式](references/handoff.md), 使用脚本生成的上下文和访问文件

这是同机本地协作的起点, 脚本保存和核对记录, Codex 工具负责实际创建任务及查询运行情况
